import type { NextConfig } from "next";

const nextConfig: NextConfig = {
   /* config options here */
  // outputFileTracingExcludes: { paths: ["./prisma/app/generated/prisma/client"] },
  }

export default nextConfig;
